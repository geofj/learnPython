# Create three variables mood, strength, and rank and assign a string to mood, a float to strength, and an integer to rank.
# The values you assign can be anything as long as they are of the correct type.

mood = "happy"
# mood variable is assigned a string
strength = 8.7
# strength variable is assigned a float
rank = 2
# rank variable is assigned an integer

print(type(mood))
print(type(strength))
print(type(rank))
# print functions to confirm value types


