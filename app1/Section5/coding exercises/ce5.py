# Create a color_codes variable and assign a tuple to it.
# The tuple should contain three tuples as items.
# Each of the three tuples should contain three items. The items can be any type.

color_codes = ((1, 2, 3), (4, 5, 6), (7, 8, 9))
# color_codes variable holds a 3 tuples inside a single tuple
print(color_codes)
# print to confirm number of tuples