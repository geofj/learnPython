# Getting user input in a batch operation using while-loop

user_prompt = "Enter a todo: "
# variable user_prompt holds the string

while True:
# creates a loop operation based on the True boolean
    todo = input(user_prompt)
    # variable todo1 calls the user_prompt string
    print(todo)
    # print function that prints the input from todos variable
    print("Next...")
    # prints cue to type in the next todos

